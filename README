
Howto run in linux:
mono app.exe

git quick referens:
git clone git://github.com/acke/MediaManagerDeamon.git

commit files:
git add "file"
git commit "file" -m "commit message"

submitt changes to shared git:
git push

